@AGENTS.md

# 役割分担（タスク別ルーティング）

コンテキスト消費とモデル性能のバランスを取るため、タスクの性質に応じて担当とモデルを振り分ける。
「何でもサブエージェントに委譲」はしない。小さな修正は割高になり、大きな調査はメイン文脈を汚すため。

## ルーティング表

| タスクの種類 | 担当 | モデル | 理由 |
|---|---|---|---|
| 方針決定・原因診断・切り分け | Claude（オーケストレーター） | Opus | 全体文脈を保持し判断の質を担保 |
| 数行〜局所の修正（場所が特定済み） | Claude が直接編集 | Opus | 新規エージェント起動の無駄を排除・即応 |
| 広い調査・横断検索 | Explore エージェント | Sonnet | 抜粋読みで安く、結論だけ返す |
| 大きめの実装・複雑なリファクタ | サブエージェント | Sonnet / Opus | 重いファイル読みをメイン文脈から隔離 |
| コマンド実行・機械的作業 | 軽量エージェント | Haiku | 推論不要で最安 |

## 運用ルール

- **小修正はメインで直接、大調査・大実装は委譲** の線引きを守る。
- **同一領域の作業は 1 つのサブエージェントを使い回す**（`SendMessage` で継続）。読み込み済みの文脈が温存され、大きなファイルの再読を避けられる。新規 `Agent` 起動は毎回コールドスタートになる点に留意。
- サブエージェントに委譲する際は、これまでに判明した事実を渡し、ゼロからの再調査を避ける。

## 構造改善の方針

- `src/components/gantt/GanttLeftPanel.tsx` は約 4000 行と巨大で、あらゆる作業でコンテキストを圧迫している。機能単位（TaskRow / 選択ロジック / 編集・保存 / キーハンドラ 等）への分割を中期目標とする。
